build32/
build64/
build/
.vs/
/relbuild
/debbuild

# Per-plugin / per-tool build outputs
plugins/*/relbuild/
plugins/*/debbuild/
plugins_unreleased/*/relbuild/
plugins_unreleased/*/debbuild/
tools/*/relbuild/
tools/*/debbuild/

# Per-user IDE state
*.vcxproj.user

# Claude Code project state (auto-memory, settings)
.claude/

# Local scrapped work (reversible archive, never released)
.backup/
